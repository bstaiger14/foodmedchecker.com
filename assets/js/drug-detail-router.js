(function () {
  const API_BASE_URL = 'https://foodmedchecker-api.curly-lake-5061.workers.dev';
  const root = document.querySelector('#drug-detail-router-root');
  const year = document.querySelector('#year');
  if (year) year.textContent = new Date().getFullYear();
  function escapeHtml(value) { return String(value || '').replace(/[&<>'"]/g, c => ({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c])); }
  function slugToName(slug) { return String(slug || '').split('-').filter(Boolean).map(w => w.charAt(0).toUpperCase() + w.slice(1)).join(' '); }
  function normalizeBadge(badge) { const v = String(badge || '').trim().toLowerCase(); return {'no special food issue found':'No special food issue found','follow label directions':'Follow label directions','use caution':'Use caution','avoid/separate':'Avoid/Separate','avoid / separate':'Avoid/Separate','avoid separate':'Avoid/Separate'}[v] || 'Follow label directions'; }
  function getBadgeClassName(badge) { return `recent-check-badge recent-check-badge-${normalizeBadge(badge).toLowerCase().replace(/[^a-z0-9]+/g, '-').replace(/^-+|-+$/g, '')}`; }
  function formatDateTime(value) { if (!value) return ''; const d = new Date(value); if (Number.isNaN(d.getTime())) return ''; return d.toLocaleDateString(undefined, { year: 'numeric', month: 'short', day: 'numeric' }); }
  function updateMeta(name) { document.title = `${name} Food Instructions | Food Med Checker`; let meta = document.querySelector('meta[name="description"]'); if (!meta) { meta = document.createElement('meta'); meta.name = 'description'; document.head.appendChild(meta); } meta.content = `Food instructions and meal timing summary for ${name}, powered by Food Med Checker.`; }
  function getSlugFromPath() { const match = window.location.pathname.match(/^\/drugs\/([^/]+)\/?$/); return match ? decodeURIComponent(match[1]) : ''; }
  function normalizeDrugCard(card, slug) { const name = String((card && (card.drugName || card.name || card.drug)) || slugToName(slug)).trim(); return { slug, drugName: name, quickAnswer: String((card && card.quickAnswer) || '').trim(), practicalTakeaway: String((card && (card.practicalTakeaway || card.practicalTips)) || '').trim(), foodSafetyBadge: normalizeBadge(card && card.foodSafetyBadge), searchedCount: Math.max(0, Number(card && card.searchedCount) || 0), firstSearchedAt: card && card.firstSearchedAt, lastSearchedAt: card && card.lastSearchedAt }; }
  function renderShell(inner) { root.innerHTML = `<section class="section drug-detail-hero"><div class="container">${inner}</div></section>`; }
  function renderNormal404() { document.title = 'Page not found | Food Med Checker'; renderShell(`<p class="eyebrow">404</p><h1>Page not found</h1><p class="hero-subtitle">We couldn’t find that FoodMedChecker page.</p><div class="drug-detail-actions"><a class="directory-button-primary" href="/">Go home</a><a class="directory-button-secondary" href="/drugs/">Browse Drugs</a></div>`); }
  function renderLoading() { renderShell('<p class="eyebrow">Medication food instructions</p><h1>Loading saved drug check…</h1><p class="hero-subtitle">Fetching the saved FoodMedChecker result.</p>'); }
  function renderNotFound(slug) { document.title = 'Saved drug check not found yet | Food Med Checker'; renderShell(`<p class="eyebrow">Medication food instructions</p><h1>Saved drug check not found yet</h1><p class="hero-subtitle">This medication may not have a saved public check yet. Run a fresh search to create one.</p><div class="drug-detail-actions"><a class="directory-button-primary" href="/?drug=${encodeURIComponent(slug)}">Search this medication</a><a class="directory-button-secondary" href="/drugs/">Back to Drug Directory</a></div>`); }
  function renderDetail(card) { updateMeta(card.drugName); const first = formatDateTime(card.firstSearchedAt); const last = formatDateTime(card.lastSearchedAt); root.innerHTML = `<section class="section drug-detail-hero"><div class="container"><p class="eyebrow">Medication food instructions</p><div class="drug-detail-title-row"><h1>${escapeHtml(card.drugName)} Food Instructions</h1><span class="${escapeHtml(getBadgeClassName(card.foodSafetyBadge))}">${escapeHtml(card.foodSafetyBadge)}</span></div><div class="drug-detail-meta"><span>Checked ${escapeHtml(card.searchedCount || 0)} ${card.searchedCount === 1 ? 'time' : 'times'}</span>${first ? `<span>First checked ${escapeHtml(first)}</span>` : ''}${last ? `<span>Last checked ${escapeHtml(last)}</span>` : ''}</div><div class="drug-detail-actions"><a class="directory-button-primary" href="/?drug=${encodeURIComponent(card.drugName)}&run=1">Run a fresh check</a><a class="directory-button-secondary" href="/drugs/">Back to Drug Directory</a></div></div></section><section class="section drug-detail-layout-section"><div class="container drug-detail-layout"><article class="drug-detail-card drug-detail-card-featured"><h2>Quick Answer</h2><p>${escapeHtml(card.quickAnswer || 'No quick answer is saved for this medication yet. Run a fresh check to rescan available labeling.')}</p></article>${card.practicalTakeaway ? `<article class="drug-detail-card"><h2>Practical Takeaway</h2><p>${escapeHtml(card.practicalTakeaway)}</p></article>` : ''}<article class="drug-detail-card"><h2>What this means</h2><p>This saved page reflects the latest FoodMedChecker result stored for this medication. Run a fresh check to rescan current available labeling and source excerpts.</p></article><article class="drug-detail-card"><h2>Need the source excerpts?</h2><p>Run a fresh check to review the live FDA label excerpts used by FoodMedChecker.</p></article><article class="drug-detail-card drug-detail-disclaimer"><h2>Educational disclaimer</h2><p>Food Med Checker summarizes FDA labeling for educational purposes only. It is not medical advice and does not replace guidance from a pharmacist, physician, or other qualified healthcare professional. Medication instructions can vary by dose, formulation, route, product, and individual patient factors.</p></article></div></section>`; }
  async function init() { const slug = getSlugFromPath(); if (!slug) return renderNormal404(); renderLoading(); try { const response = await fetch(`${API_BASE_URL}/drugs/${encodeURIComponent(slug)}`, { headers: { Accept: 'application/json' } }); if (response.status === 404) return renderNotFound(slug); if (!response.ok) throw new Error('Drug request failed'); const payload = await response.json(); let raw = payload && (payload.card || payload.drug || payload.result); if (!raw && payload && (payload.drugName || payload.name || payload.drug || payload.quickAnswer)) raw = payload; if (!raw || raw.success === false) return renderNotFound(slug); renderDetail(normalizeDrugCard(raw, slug)); } catch (e) { renderNotFound(slug); } }
  init();
}());
